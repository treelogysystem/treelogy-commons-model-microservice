spring:
  application:
    name: treelogy-commons-model-microservice
  profiles:
    active: dev
  cloud:
    config:
      uri: http://server-config.rancher.treelogy.eco.br
      fail-fast: true
      username: app
      password: my-supersecret-password
management:
  endpoints:
    web:
      exposure:
        include: '*'
